package io.getarrays.start_up.repository;

import io.getarrays.start_up.entity.HelpsPart;
import org.springframework.data.jpa.repository.JpaRepository;

public interface HelpsPartRepository extends JpaRepository<HelpsPart,Long> {
}
